{% liquid
  assign loading = 'lazy'
  assign fetch_priority = 'auto'

  if section.index == 1
    assign loading = 'eager'
    assign fetch_priority = 'high'
  endif
%}

<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<section
  id="bounce-collection-banner-{{ section.id }}"
  class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style"
  style="{% render 'spacing-style', settings: section.settings %}"
  data-testid="bounce-collection-banner-{{ section.id }}"
>
  <div
    class="bounce-collection-banner bounce-collection-banner--{{ section.settings.content_position }}"
    style="--bounce-overlay-opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }}; --bounce-height: {{ section.settings.image_height }}svh;"
  >
    <div class="bounce-collection-banner__media" aria-hidden="true">
      {% if section.settings.background_image != blank %}
        {{
          section.settings.background_image
          | image_url: width: 3200
          | image_tag:
            widths: '768, 1024, 1366, 1600, 1920, 2400, 3200',
            sizes: '(min-width: 1200px) 100vw, 100vw',
            width: section.settings.background_image.width,
            height: section.settings.background_image.height,
            alt: section.settings.title,
            class: 'bounce-collection-banner__image',
            loading: loading,
            fetchpriority: fetch_priority
        }}
      {% else %}
        <div class="bounce-collection-banner__placeholder">{{ 'hero-apparel-1' | placeholder_svg_tag }}</div>
      {% endif %}
    </div>

    <div class="bounce-collection-banner__overlay"></div>

    <div class="bounce-collection-banner__card">
      <p class="bounce-collection-banner__label">{{ section.settings.label | escape }}</p>

      {% if section.settings.title != blank %}
        <h2 class="bounce-collection-banner__title">{{ section.settings.title | escape }}</h2>
      {% endif %}

      {% if section.settings.description != blank %}
        <div class="bounce-collection-banner__description rte">
          <p>{{ section.settings.description | escape }}</p>
        </div>
      {% endif %}

      {% if section.settings.button_text != blank and section.settings.button_url != blank %}
        <a class="bounce-collection-banner__button" href="{{ section.settings.button_url }}">
          {{ section.settings.button_text | escape }}
        </a>
      {% endif %}
    </div>
  </div>
</section>

{% style %}
  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner {
    position: relative;
    min-height: clamp(520px, var(--bounce-height), 980px);
    overflow: hidden;
    display: flex;
    justify-content: flex-start;
    align-items: center;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner--left-top {
    align-items: flex-start;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner--left-bottom {
    align-items: flex-end;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner--right-center {
    justify-content: flex-end;
    align-items: center;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__media,
  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__image,
  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__placeholder,
  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__placeholder svg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__image {
    object-fit: cover;
    object-position: center;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__placeholder svg {
    background: rgba(var(--color-foreground-rgb), 0.08);
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__overlay {
    position: absolute;
    inset: 0;
    background: rgb(0 0 0 / var(--bounce-overlay-opacity));
    pointer-events: none;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__card {
    position: relative;
    z-index: 2;
    width: min(420px, calc(100% - 48px));
    margin: 32px;
    padding: clamp(24px, 2.6vw, 40px);
    background: rgb(255 255 255 / 96%);
    color: rgb(var(--color-foreground-rgb));
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__label {
    margin: 0 0 12px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    font-size: 0.75rem;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__title {
    margin: 0 0 14px;
    font-size: clamp(1.7rem, 2.7vw, 2.35rem);
    line-height: 1.1;
    text-transform: uppercase;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__description {
    margin: 0 0 24px;
    max-width: 32ch;
    line-height: 1.55;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__description p {
    margin: 0;
  }

  #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__button {
    display: inline-block;
    padding: 12px 20px;
    background: #000;
    color: #fff;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.74rem;
    font-weight: 600;
    line-height: 1;
  }

  @media screen and (max-width: 749px) {
    #bounce-collection-banner-{{ section.id }} .bounce-collection-banner {
      min-height: auto;
      display: grid;
      grid-template-rows: minmax(300px, 48svh) auto;
      align-items: stretch;
      justify-content: stretch;
    }

    #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__media,
    #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__overlay {
      position: relative;
      grid-row: 1;
      min-height: 300px;
    }

    #bounce-collection-banner-{{ section.id }} .bounce-collection-banner__card {
      grid-row: 2;
      width: 100%;
      margin: 0;
      padding: 24px;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "Bounce Collection Banner",
  "tag": "section",
  "class": "section-bounce-collection-banner",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background image"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Small label",
      "default": "OUR HAIR"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "BOUNCE COLLECTION"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Our collection of steam-textured virgin Indian hair offering a variety of patterns for every hair type. These steamed textures were designed to match every kink and curl for the perfect blend."
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "SHOP BOUNCE"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button URL"
    },
    {
      "type": "range",
      "id": "image_height",
      "label": "Image height",
      "min": 50,
      "max": 100,
      "step": 5,
      "unit": "vh",
      "default": 70
    },
    {
      "type": "select",
      "id": "content_position",
      "label": "Content position",
      "options": [
        {
          "value": "left-center",
          "label": "Left center"
        },
        {
          "value": "left-top",
          "label": "Left top"
        },
        {
          "value": "left-bottom",
          "label": "Left bottom"
        },
        {
          "value": "right-center",
          "label": "Right center"
        }
      ],
      "default": "left-center"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay opacity",
      "min": 0,
      "max": 70,
      "step": 1,
      "unit": "%",
      "default": 12
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        {
          "value": "page-width",
          "label": "Page"
        },
        {
          "value": "full-width",
          "label": "Full"
        }
      ],
      "default": "full-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "Bounce Collection Banner",
      "category": "Image"
    }
  ]
}
{% endschema %}
